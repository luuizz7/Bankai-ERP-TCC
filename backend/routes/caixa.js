import { Router } from 'express';
import pool from '../db.js'; // Removido as chaves {} de pool

const router = Router();

// ... o resto do seu código de rotas continua igual ...

export default router;